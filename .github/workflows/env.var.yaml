name: Enviroments variables
on: [push]

jobs:
  env-vars-and-context:
    runs-on: ubuntu-latest
    steps:
      - name: Log env variables
        run: echo $GITHUB_REF
      - name: Log context
        run: echo '${{github.ref}}'